export interface Data {
    parts: Part[];
}

export interface Part {
    name: string;
    characteristics: string;
    uses: string;
    triggers: string;
    emotional_distance: string;
    notes: string;
    episodes: string;
}
